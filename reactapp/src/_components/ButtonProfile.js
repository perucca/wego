import React from 'react';

export const ButtonProfile = (props) => (
    <div className="col-12">
            <a href={props.type} className="btn btn-info btn-lg my-1" role="button">{props.name}</a>
        </div>


)
